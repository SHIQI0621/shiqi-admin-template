export namespace STATES {
    let INITIAL: number;
    let READY: number;
    let ATTACHING: number;
    let ATTACHED: number;
    let NOTALLOW: number;
    let RUNNING: number;
    let ENDED: number;
    let DESTROYED: number;
}
export const STATE_ARRAY: string[];
